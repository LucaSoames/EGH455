# Essential backend dependencies for UAV Payload System with Auth + Video
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-CORS==4.0.0
Flask-SocketIO==5.3.6
# Flask-JWT-Extended==4.6.0  # Removed for simplicity
Werkzeug==3.0.1
SQLAlchemy==2.0.23
python-socketio==5.11.0
python-engineio>=4.8.0
python-dotenv==1.0.0
requests==2.31.0

# For video streaming
opencv-contrib-python-headless>=4.9,<5

# Removed heavy dependencies:
# numpy==1.26.2          # Only if doing data processing
# pandas==2.1.4          # Only if doing data analysis  
# marshmallow==3.20.1    # Only if using complex serialization
# flask-marshmallow==0.15.0
# marshmallow-sqlalchemy==0.29.0
# geopy==2.4.1           # Only if doing location calculations
# gunicorn==21.2.0       # Only for production deployment
# redis==5.0.1           # Only if using Redis
# celery==5.3.4          # Only if using background tasks
# pydantic==2.5.2        # Only if using Pydantic validation